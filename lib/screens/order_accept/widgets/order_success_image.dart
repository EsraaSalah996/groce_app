import 'package:flutter/material.dart';

class OrderSuccessImage extends StatelessWidget {
  const OrderSuccessImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/images/accepted.png", width: 250);
  }
}
